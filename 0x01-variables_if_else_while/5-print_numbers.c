#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point 5 print numbers
 *
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar(10);
	return (0);
}
