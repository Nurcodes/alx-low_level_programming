#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @n: letter being tested
 * @i: letters being tested
 * Return: Always 0
 */
int add(int n, int i)
{
	int result = n + i;

	return(result);
}
